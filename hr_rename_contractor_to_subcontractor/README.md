# Windweaver Odoo Customizations - HR

## Included changes

- Renames the displayed `hr.employee.employee_type` option `Contractor` to `Subcontractor`.
- Keeps the stored technical value as `contractor` for compatibility.

## Optional dependency behavior

This module depends on:

- `hr`
